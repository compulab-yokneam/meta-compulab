# We have a conf and classes directory, append to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have a recipes directory containing .bb and .bbappend files, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "compulab-core-main"
BBFILE_PATTERN_compulab-core-main := "^${LAYERDIR}/"
BBFILE_PRIORITY_compulab-core-main = "4"

LAYERSERIES_COMPAT_compulab-core-main = "wrynose walnascar"
